module.exports = {
  collectionName: "My API",
  workspaceName: "My Workspace", // Workspace name is optional, if not provided, it will use the default workspace
  // Use either url or filePath, not both
  url: "http://127.0.0.1:8000/swagger.json",
  // filePath: "/path/to/swagger.json"
};
